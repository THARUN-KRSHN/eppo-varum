'use client';

import { Bell, Check, Clock3, Info, MapPin, Play } from 'lucide-react';
import { useState } from 'react';

const api = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api';

export default function Alerts() {
  const [message, setMessage] = useState('');
  const [enabled, setEnabled] = useState(false);
  const trigger = async () => {
    const response = await fetch(`${api}/notifications/demo-trigger`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ stop_id: '2', advance_minutes: 30 }) });
    const data = await response.json();
    setMessage(data.message || 'Demo reminder triggered.');
  };
  return <main className="mx-auto max-w-5xl px-5 pb-28 pt-32 md:pt-40"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[.18em] text-yellow-700">Reminder demo</p><h1 className="mt-3 text-4xl font-bold tracking-tight text-green-950 md:text-6xl">Bus reminders</h1><p className="mt-4 text-lg text-green-950/60">This screen demonstrates the reminder flow before live push notifications are connected.</p></div><div className="mt-10 grid gap-6 md:grid-cols-[1fr_.82fr]"><section className="surface p-7 md:p-9"><div className="flex items-start justify-between"><div><span className="inline-flex items-center gap-2 rounded-full bg-yellow-100 px-3 py-1 text-xs font-bold text-green-950"><span className="h-2 w-2 animate-pulse rounded-full bg-yellow-600"/> Demo only</span><h2 className="mt-6 text-2xl font-bold text-green-950">Mannuthy to Ernakulam</h2><p className="mt-2 text-sm text-green-950/60">A sample reminder for the next scheduled bus.</p></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-green-100 text-green-700"><Bell size={23}/></div></div><div className="mt-8 space-y-3"><div className="flex items-center gap-3 rounded-2xl border border-green-100 p-4"><MapPin className="text-green-700" size={19}/><div><p className="text-xs text-green-950/50">Stop</p><p className="font-bold">Mannuthy · മണ്ണുത്തി</p></div></div><div className="flex items-center gap-3 rounded-2xl border border-green-100 p-4"><Clock3 className="text-green-700" size={19}/><div><p className="text-xs text-green-950/50">Sample arrival</p><p className="font-bold">07:20 · reminder at 06:50</p></div></div></div><button onClick={() => setEnabled(!enabled)} className={`mt-7 inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-full font-bold ${enabled ? 'bg-green-100 text-green-800' : 'bg-green-700 text-white hover:bg-green-800'}`}>{enabled ? <><Check size={18}/> Demo reminder enabled</> : <><Bell size={18}/> Enable demo reminder</>}</button>{message && <p className="mt-4 rounded-xl bg-green-50 p-3 text-sm font-semibold text-green-800">{message}</p>}</section><aside className="space-y-5"><section className="rounded-3xl bg-green-800 p-7 text-white"><div className="flex items-center gap-3"><Info size={21}/><h2 className="text-xl font-bold">How the demo works</h2></div><ol className="mt-5 space-y-4 text-sm leading-6 text-white/80"><li><strong className="text-white">1.</strong> Choose a bus stop on the map and select its timing.</li><li><strong className="text-white">2.</strong> Tap “Alert me 30 min before” to save a reminder.</li><li><strong className="text-white">3.</strong> This page can trigger a sample response so the flow can be tested.</li></ol><p className="mt-5 border-t border-white/15 pt-4 text-xs leading-5 text-white/65">Currently this is a showcase: the browser notification is local and the backend stores the subscription, but there is no production push service or background delivery worker yet.</p></section><button onClick={trigger} className="inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-full border border-green-200 bg-white font-bold text-green-800 hover:bg-green-50"><Play size={17}/> Trigger sample reminder</button></aside></div></main>;
}
