'use client';
import Link from 'next/link'; import { FileText, LogOut, ShieldCheck } from 'lucide-react'; import { useEffect, useState } from 'react'; import { useAuth } from '../../components/AuthProvider';
const api = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api';
export default function Profile() { const { user, token, ready, signOut } = useAuth(); const [documents, setDocuments] = useState<any[]>([]); useEffect(() => { if (token) fetch(`${api}/documents`, { headers: { Authorization: `Bearer ${token}` } }).then(response => response.json()).then(data => setDocuments(data.data || [])); }, [token]); if (!ready) return null; if (!user) return <main className="mx-auto max-w-md px-5 pt-40 text-center"><h1 className="text-3xl font-bold">Sign in to see your history.</h1><Link href="/login" className="mt-6 inline-flex rounded-full bg-green-700 px-6 py-3 font-bold text-white">Sign in</Link></main>; return <main className="mx-auto max-w-5xl px-5 pb-28 pt-32 md:pt-40"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-green-600">Your eppo varum</p><h1 className="mt-3 text-4xl font-bold text-green-950">{user.username}</h1><p className="mt-2 text-green-950/60">{user.email}</p></div><button onClick={signOut} className="inline-flex min-h-11 items-center gap-2 rounded-full border border-green-200 px-4 font-bold text-green-800"><LogOut size={17}/> Sign out</button></div><section className="mt-10"><div className="flex items-center gap-3"><ShieldCheck className="text-green-700" size={21}/><h2 className="text-xl font-bold text-green-950">Upload history</h2></div><div className="mt-5 space-y-3">{documents.length ? documents.map(document => <Link href={`/verify?document=${document.id}`} key={document.id} className="surface flex items-center gap-4 p-5"><FileText className="text-green-700" size={22}/><div className="min-w-0 flex-1"><p className="truncate font-bold text-green-950">{document.original_filename}</p><p className="text-sm text-green-950/55">{document.status} · {new Date(document.created_at).toLocaleString()}</p></div><span className="rounded-full bg-green-100 px-3 py-1 text-xs font-bold text-green-800">{document.file_type}</span></Link>) : <div className="surface p-8 text-center text-green-950/60">No uploads yet. <Link href="/upload" className="font-bold text-green-700">Upload a timetable.</Link></div>}</div></section></main>; }
